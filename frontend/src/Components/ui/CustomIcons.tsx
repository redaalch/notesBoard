import { type SVGProps } from "react";

/**
 * Custom SVG icons that match the LucideIcon type signature.
 *
 * Use this file when Lucide lacks a specific icon you need.
 * Each icon should be a forwardRef SVG component so it can be used
 * anywhere `AppIcon` / `LucideIcon` is expected (typed interfaces,
 * dynamic icon maps, toolbar buttons, etc.).
 *
 * Template:
 *
 *   export const MyCustomIcon = forwardRef<SVGSVGElement, SVGProps<SVGSVGElement>>(
 *     ({ className, ...props }, ref) => (
 *       <svg
 *         ref={ref}
 *         xmlns="http://www.w3.org/2000/svg"
 *         width="24"
 *         height="24"
 *         viewBox="0 0 24 24"
 *         fill="none"
 *         stroke="currentColor"
 *         strokeWidth="2"
 *         strokeLinecap="round"
 *         strokeLinejoin="round"
 *         className={className}
 *         {...props}
 *       >
 *         {/* SVG paths here *\/}
 *       </svg>
 *     ),
 *   );
 *   MyCustomIcon.displayName = "MyCustomIcon";
 */

// Add custom icons below as needed.
// This file intentionally starts empty.
