{
  "name": "notesboard",
  "version": "1.0.0",
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "postinstall": "npm ci --prefix backend",
    "heroku-postbuild": "npm ci --include=dev --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  }
}
